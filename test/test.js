
// test : abec : simple test that runs both clientside and serverside
// ----------------------------------------------------------------------------------------------------------------------------
    if ((typeof window) == "undefined")
    {
        require("./abec.js");
        server.create("127.0.0.1:2592",disk.root,"/test.htm");
    }

    dump(ENVITYPE);
// ----------------------------------------------------------------------------------------------------------------------------
